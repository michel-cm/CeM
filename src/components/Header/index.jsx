import * as C from "./styles";
import ToggleTheme from '../ToggleTheme'

const Header = ( ) => {

  return (
    <C.Container>
      Logo
    <ToggleTheme />
    </C.Container>
  );
};

export default Header;
